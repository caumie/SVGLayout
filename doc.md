

基本は、foreignObjectタグを使う。IEがなくなったためできる芸当だ。
この中ならhtmlのほとんどが使えるはず
ただし、xmlnsを設定しないと表示されないなどの制限はある

attribute
data-nameでidの代わりに重複してもよい要素とする
data-forでどこの要素を参照したかを考えるとよい

追加するのは指定要素の直後だろう
foreignObjectタグのx, y, width, heightを同一にすることで同一の範囲で表示させる
rectの線の太さなどで文字と重なることに関しては太さからの計算でpaddingができると思う
ただし何かの設定ファイルで利用者の意図を反映設定できるようにもしたい。
重なりなどはsvg側で考えることになる

入れ込む要素は、テキスト、画像、svg
テキストはforeignObjectの後でdivなどで簡易にできる。折り返しが使えるのが大きい。
画像はimageタグで直接svgに記載できる。外枠などはsvg側に任せる。
svgのインラインはforignObjectに入れ込んだ方が楽だろう。
それなら画像もforeignObjectが良いとも思う。

線の太さを指定した時には
上に乗せるforign>divに対してstyle="margin:4;"などとうつ、単位を考えなくてもよくなるかもしれない。
